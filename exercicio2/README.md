Explicação:

mkdir -p → Cria o diretório /etc/ssl/certificates. O -p garante que, se os diretórios anteriores não existirem, eles serão criados.

cd /etc/ssl/certificates → Entra no diretório criado.

Esse diretório será usado para armazenar as chaves e certificados gerados, mantendo a organização.
